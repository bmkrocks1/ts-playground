(() => {

  function findLocation(coordinates: [number, number]) {
    //
  }
  
  /*
    Solution 1:
    
    Add type annotation to `place`.
  */
  const place: [number, number] = [41.40338, 2.17403];
  
  findLocation(place);

})();